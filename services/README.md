# Services

The modules in this directory are intentionally independent of the crawler
implementations.  This keeps filtering and Sheets synchronization testable
without network access or Google credentials.
